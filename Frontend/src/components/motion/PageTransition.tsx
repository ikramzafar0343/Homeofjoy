"use client";

/**
 * Route transitions are handled by SitePreloader (brand screen on every
 * navigation). This stub remains so existing imports keep working.
 */
export default function PageTransition() {
  return null;
}
